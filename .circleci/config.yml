version: 2.1
jobs:
  build-and-test-compiler:
    docker:
      - image: silkeh/clang
    steps:
      - checkout
      - run:
          name: "Configure"
          command: "meson setup build"
workflows:
  build-and-test-compiler-workflow:
    jobs:
      - build-and-test-compiler
